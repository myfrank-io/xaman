# Real directories instead of symlinks into .pnpm (D92): the OCR worker of the inbox is loaded by
# file path at run time, and Vercel refuses a serverless package whose traced files sit behind a
# symlinked directory. The lockfile is unaffected.
node-linker=hoisted
